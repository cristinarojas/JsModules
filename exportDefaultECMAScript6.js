// Declaring my module
var utils = {
	generateRandom: function() {
		return Math.random();
	},
	sum: function() {
		return a + b;
	}
};


// Export a single value from the module, I jus export the object utils
export default utils;